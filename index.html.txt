<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TEIXEIRA SYSTEM</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>

<header>
    <h1>TEIXEIRA SYSTEM</h1>
    <p>Sistema de Controle de Incidentes de TI</p>
</header>

<nav id="menu">
    <button type="button">Dashboard</button>
    <button type="button">Registrar</button>
    <button type="button">Monitorar</button>
</nav>

<main>
    <!-- Dashboard -->
    <section id="dashboard" class="secao">
        <h2>Dashboard</h2>
        <div id="contadores">
            <div class="contador"><h3>Aberto</h3><span id="count-aberto">0</span></div>
            <div class="contador"><h3>Em andamento</h3><span id="count-andamento">0</span></div>
            <div class="contador"><h3>Resolvido</h3><span id="count-resolvido">0</span></div>
        </div>
        <canvas id="graficoStatus"></canvas>
    </section>

    <!-- Registrar -->
    <section id="registrar" class="secao">
        <h2>Registrar Novo Incidente</h2>
        <div class="form-registro">
            <input type="text" id="titulo" placeholder="Título do incidente">
            <textarea id="descricao" placeholder="Descrição do incidente"></textarea>
            <select id="status">
                <option value="Aberto">Aberto</option>
                <option value="Em andamento">Em andamento</option>
                <option value="Resolvido">Resolvido</option>
            </select>
            <button type="button" id="btnRegistrar">Registrar</button>
        </div>
    </section>

    <!-- Monitorar -->
    <section id="monitorar" class="secao">
        <h2>Monitorar Incidentes</h2>
        <div class="filtros">
            <input type="text" id="buscar" placeholder="Buscar por título...">
            <select id="filtroStatus">
                <option value="Todos">Todos</option>
                <option value="Aberto">Aberto</option>
                <option value="Em andamento">Em andamento</option>
                <option value="Resolvido">Resolvido</option>
            </select>
        </div>
        <div id="cards-incidentes"></div>
    </section>
</main>

<!-- Modal -->
<div id="modal" class="modal">
    <div class="modal-content">
        <span class="close">&times;</span>
        <h2>Editar Incidente</h2>
        <input type="text" id="modal-titulo">
        <textarea id="modal-descricao"></textarea>
        <select id="modal-status">
            <option value="Aberto">Aberto</option>
            <option value="Em andamento">Em andamento</option>
            <option value="Resolvido">Resolvido</option>
        </select>
        <button id="btnSalvarEdicao">Salvar</button>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script src="script.js"></script>
</body>
</html>
